Target: fresh 3.4.2.6 + AutoISF 3.2.1 (the AndroidAPS-3426 clone at
3.4.2.6+aisf3.2.1). Paths match AAPS 3.4.x (app.aaps.*). This will not
apply to AAPS 3.2.0.4 (info.nightscout.* layout).

From the clone root:

  git apply --check ..\AaAPS3422a320\patches\bolus-calculator-on-3426-aisf321.patch
  git apply ..\AaAPS3422a320\patches\bolus-calculator-on-3426-aisf321.patch

If the clone is elsewhere, pass the full path to the .patch file.

After apply, turn on Overview preference "Enable delayed bolus" for the
50%-profile / Walking soon top-up path. Split-bolus and protein/fat
follow-ups are per-bolus checkboxes, not that preference.
==========================
Manual split-bolus progress and recalculation. Before each later part the code verifies that the user has not stopped follow-up dosing, no newer bolus/carbs entry has superseded the schedule, profile remains at least 100%, the pump is available, and superbolus is not active. It obtains fresh glucose. Missing data is retried after two minutes. BG below 6.0 mmol/L is unsafe; from 6.0 to below 8.0 mmol/L, a delta or short-average delta at or below -0.05 mmol/L is also unsafe. The first two consecutive unsafe checks defer by a full split interval; the third cancels the residual. The complete soft-retry window is limited to 60 minutes. Each part is reduced by any positive rise in live IOB since the preceding post-dose baseline and rounded to the pump step. A result at or below zero is skipped and retried at the next interval rather than increasing the dose. Success schedules the next residual; failure raises the normal delivery alarm. Cancellation and calculated-zero outcomes are written to treatment/history markers.
===============

Ran 4 commands

Done. kmp (4.0.0-dev-b-kmp, from nightscout/AndroidAPS) is now on RSC-RJCgit/AndroidAPS-aisf320.

Its branch-ci.yml is the same CI family as your fork — same secret names (KEYSTORE_BASE64/_PASSWORD, KEY_ALIAS/_PASSWORD, GDRIVE_OAUTH2), same Drive upload. Your repo secrets apply, nothing to re-enter.

To build:

gh workflow run branch-ci.yml --repo RSC-RJCgit/AndroidAPS-aisf320 --ref kmp -f buildVariant=fullRelease

or Actions → Branch CI → Run workflow → Branch kmp, Variant fullRelease.

Both branches now on your fork:

Branch	What	CI
rjc-boost	Boost v3.4.2.6-boost-v6 release	Branch CI ✓
kmp	AAPS 4.0.0-dev-b-kmp (v4 / Kotlin-Multiplatform)	Branch CI ✓

Caveats for kmp:

CI uses .github/jdk-map.json .default for the JDK — the kmp branch sets that itself, runner handles it.
It's a v4 dev branch — the CI build can fail for the same tooling reasons a local one might. A red run there ≠ your config, just upstream churn.
Delete later with git push aisf :kmp / git push aisf :rjc-boost.

Remotes in the AndroidAPS3 checkout: origin → nightscout, aisf → your fork. Local kmp tracks aisf/kmp now (not origin/kmp) — if you want to pull upstream v4 updates, git pull origin kmp explicitly.
========================
C:\backup\AAPS\aiv_Client
C:\backup\AAPS\logs_Client
C:\backup\AAPS\aapsLogs\Client
============================

can you show me code now for initial bolus, subsequent bolus , suppression, timing etc?
=======================
What to do in Android Studio (on AndroidAPS-master-boluscalc, not this fork)

File → Sync Project with Gradle Files and wait until it finishes.
If the errors stay, File → Invalidate Caches → Invalidate and Restart.
Confirm the project root is C:\Users\arjay\StudioProjects\AndroidAPS-master-boluscalc (the Gradle wrapper / settings.gradle folder), not a parent StudioProjects folder and not this AaAPS3422a320 tree.
Until that sync completes, a newly added layout such as dialog_quick_wizard_max_bolus.xml is especially likely to show this, because view binding (DialogQuickWizardMaxBolusBinding) does not exist until the Android plugin has processed res/layout.

No XML edits are required for these errors. 

]===================
1. can you put this repo on GitHub?
2. can you make a new patch of just these changes from origin master?
=============================
new upload
cause of hypo?
faster insulin this pod..
change peak?
decrease SMBdelivery and BMild boost?

===============

only use BMildDay in evening if bgl>8.0 and Deltas still high?[over 0.4?] [or was it BMild?]

=================
