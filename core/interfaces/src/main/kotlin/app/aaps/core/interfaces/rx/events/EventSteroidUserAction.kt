package app.aaps.core.interfaces.rx.events

/** Direct Overview-button/menu command handled by the active AutoISF Kotlin plugin. */
class EventSteroidUserAction(val directMenu: Boolean = false) : Event()
